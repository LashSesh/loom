[package]
name = "cce-phaseblock"
version = "0.1.0"
edition = "2021"
license = "SEE-REPO-ROOT"
publish = false

[dependencies]
cce-core = { path = "../cce-core" }
cce-kernel = { path = "../cce-kernel" }
