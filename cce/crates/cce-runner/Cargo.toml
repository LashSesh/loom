[package]
name = "cce-runner"
version = "0.1.0"
edition = "2021"
license = "SEE-REPO-ROOT"
publish = false

[dependencies]
cce-core = { path = "../cce-core" }
cce-crystal = { path = "../cce-crystal" }
cce-kernel = { path = "../cce-kernel" }
cce-phaseblock = { path = "../cce-phaseblock" }
cce-spiral = { path = "../cce-spiral" }
cce-phc = { path = "../cce-phc" }
cce-loom = { path = "../cce-loom" }
cce-observe = { path = "../cce-observe" }
cce-materialize = { path = "../cce-materialize" }
# S-E5 §10(g): der Aktivierungs-Hook (norm_profile) — Abhaengigkeits-
# richtung cce-runner -> cce-bridge (kein Zyklus: cce-bridge kennt
# cce-runner nicht).
cce-bridge = { path = "../cce-bridge" }
