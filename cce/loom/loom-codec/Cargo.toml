[package]
name = "loom-codec"
version = "0.1.0"
edition = "2021"
license = "SEE-REPO-ROOT"
publish = false

# Motorfrei (Reader-/Writer-Kern des Formats).
[dependencies]
loom-canon = { path = "../loom-canon" }
loom-format = { path = "../loom-format" }
