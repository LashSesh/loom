# 23 — AUTONOMES FORTSCHRITTSPROGRAMM „VOLLE KRAFT" (Augen · P5 RepoIntelligence · Politur)

**Anlass:** Der reale Fremdwerkzeug-Vergleich (Dokument 22, externer Arm) ist auf Auftraggeber-Entscheidung **zurückgestellt** — die Bedienlast läge beim Menschen, und das ist derzeit keine gute Lösung. Die gebauten P4-Ext-Bausteine bleiben; der echte Lauf wird im Register umklassifiziert: *offen, optional, jederzeit nachholbar, blockiert nichts* (K9 gilt in der P4-Fassung; der wörtliche Marktvergleich bleibt sichtbar offen — kein Overclaim). **Direktive:** autonomes Weiterarbeiten in Etappen, ohne jede Bedienlast beim Auftraggeber. Normativer Overlay; Reihenfolge Track A → B → C; Stopps nur wo unten benannt.

## Track A — AUGEN (höchste Priorität: der Auftraggeber soll das System zum ersten Mal SEHEN)

**A1 · Windows-Cross-Build-Versuch:** Ziel `x86_64-pc-windows-gnu` (mingw-w64) für `cockpit-app` (Renderer-Backend wählen, das cross am robustesten ist — glow bevorzugt; wgpu nur falls nötig) und `loom`-CLI. Wenn erfolgreich: `dist/cce-windows.zip` = `cockpit.exe` + `loom.exe` + `library/seed/*.loom` + `ERSTSTART.txt` (exakt drei Schritte, null Terminal: 1. ZIP entpacken · 2. Doppelklick auf cockpit.exe · 3. die Saat-Reise fahren — Formulierung aus Handbuch §1/§9 kondensiert, Claim-Schranke beachtet). Committen/pushen, damit der Auftraggeber es direkt von GitHub laden kann. Wenn der Cross-Build scheitert: präziser Befund je Fehlstelle (Linker? egui-Backend? Font-Stack?), ehrlich als host-gebunden dokumentiert, KEIN Hack-Umweg. **A2 · Linux-Paket auffrischen** (bestehende `ci/package.sh`, aktueller Stand, gleiche ERSTSTART-Datei). **A3 ·** macOS bleibt ehrlich host-gebunden (Cross ohne SDK nicht seriös möglich) — nur vermerken.

## Track B — P5 „RepoIntelligence" (die Fähigkeit, die kein Marktwerkzeug hat)

**Ziel:** CCE bearbeitet ein Repository nicht nur — es **versteht es beweisbar**: liest ein *fremdes* Repo über die bestehenden CSA-Adapter (Git-/LocalCorpus, Lizenz-/Policy-Gates unverändert scharf), destilliert dessen Strukturmuster über die bestehende HBM-Kette, kompiliert daraus ein `GroundingPacket` (Dokument 21) und versiegelt das Ganze als zertifizierten **Bauplan-Workbody**: Blueprint-Kristall + Regeln + offene Entscheidungen + `cites` auf die Quell-Evidence, `verify == Valid`, replay-identisch. **Zeuge R-RIG-1** (Betriebsverifikation wie P1/P3/P4, hermetische Strukturzeugen daneben). **STOPP vor dem echten Lauf:** konkretes Ziel-Repo vorschlagen (klein, permissiv lizenziert — der projekteigene `numkit`-Testling ist als Erstziel zulässig und lizenzfrei unbedenklich; ein externes MIT-Repo nur nach Freigabe) und Freigabe abwarten. Kein Netz-Fetch ohne die volle CSA-Kette.

## Track C — POLITUR (eigenes Ermessen, klein, ohne Host-Abhängigkeit)

**C1 ·** Kanzel-Zweitmanifest `gpt-4o` (stärkeres Formen; konservatives Budget; umschaltbar, `gpt-4o-mini` bleibt Default) — nutzt vorhandenes Guthaben, keine neue Architektur. **C2 ·** Kanzel-Prompt-Bibliothek (IG-R4): versionierte `system_contract`-Vorlagen je Einsatz (Wunsch-Formung, Erklärung, Reparaturvorschlag), als geprüfte Assets. **C3 ·** Register-Konsolidierung: P4-Ext-Umklassifizierung eintragen; jede Nummer mit Endstatus. **C4 ·** danach 2–3 kleinste offene Registerpunkte eigenen Ermessens (ohne Host, ohne Netz außer dem freigegebenen OpenAI-Pfad) — Wahl im Bericht begründen.

## Regeln und DoD

Kontingent-Disziplin wie eingeführt (kleiner Commit je Einheit; jeder Abbruch verlustarm). Berichte: `reports/A_augen_bericht.md`, `reports/P5_repointelligence_bericht.md`, `reports/C_politur_bericht.md` + `VOLLAUSBAU_STATUS` nachgeführt. Stopps NUR: (1) vor dem echten P5-Lauf (Repo-Wahl), (2) falls Track A Toolchain-Bestandteile jenseits der erlaubten Quellen bräuchte — dann melden statt beschaffen. `spec/` unangetastet; alle bisherigen Tore/Gates/Zeugen unverändert; bei Unklarheit Residuum statt Raten. **DoD(Programm) = 1 ⟺** Track A abgeschlossen (Erfolg ODER präziser host-gebundener Befund) ∧ R-RIG-1 real erbracht ∧ Track C1–C3 erledigt ∧ Alt-Zeugen unverändert ∧ CI GRUEN.
